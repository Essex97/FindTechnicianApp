<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="gr.aueb.softeng.project1804">

    <application
        android:name=".memorydao.TechnicianDAOMemory"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.main_screen.MainScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.login_screen.LogInActivity" />
        <activity android:name=".view.signup_screen.SignUpActivity" />
        <activity android:name=".view.signup_categories_screen.SignUpCategoriesActivity" />
        <activity android:name=".view.service_assign_screen.ServicesAssignActivity" />
        <activity android:name=".view.HomeScreenCustomerActivity.HomeScreenCustomerActivity" />
        <activity android:name=".view.info.TechnicianInfoActivity"></activity>
    </application>

</manifest>